file:
  directory: D:/ksj/download/greengram_Tdd

spring:
  datasource:
    url: jdbc:log4jdbc:mariadb://localhost/greengram_ver3_test
    username: root
    password: green502
  sql:
    init:
      mode: always #resourse아래에 있는 schema.sql(table생성 DDL) , data.sql(데이터 insert DML)파일을 프로젝트(테스트)시작시 실행.